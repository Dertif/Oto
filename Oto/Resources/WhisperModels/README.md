Place bundled WhisperKit model files in this directory.

Phase 0.1 expectation:
- No runtime download for Whisper models.
- App should ship with model assets inside the bundle.
- Fixed model for Phase 0.1: `base`.

Example structure can vary by chosen model variant; keep the full model folder contents here.
